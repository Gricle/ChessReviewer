import shell from './shell.json';
import importNs from './import.json';
import review from './review.json';
import coach from './coach.json';
import library from './library.json';
import seo from './seo.json';

/**
 * French resource bundle: one entry per namespace.
 * (`import` is a reserved word, hence the `importNs` alias.)
 */
export const fr = {
  shell,
  import: importNs,
  review,
  coach,
  library,
  seo,
};
